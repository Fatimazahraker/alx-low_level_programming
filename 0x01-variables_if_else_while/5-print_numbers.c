#include <stdio.h>
/**
 * main - this is a fct that prints alphabets exept e and q
 * Return: 0 (success)
 */
int main(void)
{	int i;
	for (i = 0; i < 10 ; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}
