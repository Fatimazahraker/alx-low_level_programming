#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers.
 * @a: first integer a
 * @b: second integer b
 *
 * Return: retun  m integer witch is multiple of a and b
 */
int mul(int a, int b)
{
	int m;

	m = a * b;

	return (m);
}
