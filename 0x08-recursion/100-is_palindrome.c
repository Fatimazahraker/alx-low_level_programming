#include "main.h"
#include <stdio.h>

/**
 * is_palindrome - ckeck if a string is a palindrome
 * @s: string
 * Return: return 0 or 1
 */

int is_palindrome(char *s)
{
	while (s[i] != '\0')
	{
		i++;
	}

	if (l != i / 2)
	{
		if 

